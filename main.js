var typed = new Typed('.multiple-texts', {
    strings: ['FrontEnd Web Developer', 'Youtuber', 'Freelancer'],
    typeSpeed: 100,
    backSpeed: 100,
    baclDelay: 1000,
    loop: true
})

const title = document.getElementById('title');
console.log(title);